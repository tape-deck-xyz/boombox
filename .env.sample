AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
STORAGE_REGION=
STORAGE_BUCKET=
# Optional: hostname / URL host for GET /info JSON and catalog rebuilds (e.g. behind a reverse proxy). See docs/library-catalog-and-info.md
# PUBLIC_HOSTNAME=
# Admin Basic Auth (for GET /admin and POST / uploads). Leave empty to disable admin.
ADMIN_USER=
ADMIN_PASS=